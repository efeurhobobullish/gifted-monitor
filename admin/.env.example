# Admin (build-time; used by Vite)
# Copy to .env and set before building for production.
# Admin can also set/update API URL from Dashboard → API Settings (saved in browser).

# Backend API base URL (no trailing slash). Optional; can be set in API Settings UI.
# Example: https://your-api.herokuapp.com
VITE_BASE_URL=http://localhost:4001

# Paystack public key (if using payments)
# VITE_PAYSTACK_PUBLIC_KEY=pk_live_...
